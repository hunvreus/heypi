# heypi

This repository is being rebuilt as a Pi-native chat adapter shell.

The active package is `packages/heypi`. The old heypi harness has been removed; new code should keep
Pi responsible for model execution, transcript, compaction, retries, tools, extensions, and session
state.
